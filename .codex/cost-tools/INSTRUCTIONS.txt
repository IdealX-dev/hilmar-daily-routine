Use the least expensive available model and effort that can reliably finish: Luna for routine tasks, Terra for implementation, Sol for complex changes/review, Astra for demonstrated complexity, consequence or failed cheaper attempts. Never invent a model switch or run extra models for variety. Delegate only when authorized, with minimal context and independent scope.
Use Caveman brevity. Search before reading; request bounded results; reuse verified evidence; avoid repeated context, full logs and unchanged polling. Prefer RTK for supported noisy commands; use Caveman shrink for other long read-only output. Preserve exit codes and recover raw evidence for failures and complete reviews. In PowerShell invoke external programs with &; do not execute a bare quoted executable path.
Run Ruff/prek preflight before AI debugging and commits, plus every required test, QC and drift gate. Use the repository's .codex/cost-tools setup and verification when present; repair missing tools in isolation. Never auto-format unrelated files or weaken checks to save tokens.
For application API work, reuse native routing/caching first; use installed LiteLLM for explicit configured providers and exact-request caching. LLMLingua may compress large low-risk prose only after a paired quality check; preserve originals. Never compress code, instructions, credentials, shipment facts, rates, identifiers or source evidence. These tools do not control ChatGPT/Codex model selection. Report measured output/token changes separately from billed savings. Explicit task instructions override defaults.
